services:
  web:
    build:
      context: ${CONTEXTO}
      dockerfile: ./dockerfiles/node/node
    container_name: ${NOMBRE}
    ports:
      - "${PORT_WEB}:80"     
    env_file:
      - ./.env
    image: jiahaolin69/node
    volumes:
      - ./logs:/root/logs      
    networks:
      netmegacrossovernode:
        ipv4_address: ${IP}
networks:
  netmegacrossovernode:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: ${SUBNET}